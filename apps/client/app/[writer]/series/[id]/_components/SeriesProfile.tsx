import type { SeriesDto } from "@moonjin/api-types";

export default function SeriesProfile({
  seriesInfo,
}: {
  seriesInfo: SeriesDto;
}) {
  return <header className="w-full h-[200px]  flex items-center"></header>;
}
